@model ShoppingCartViewModel

<h2>Shopping Cart</h2>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            @*&nbsp; is non-breakable space*@
            <th>&nbsp;</th>
            <th>Selected Amount</th>
            <th>Items</th>
            <th class="text-right">Price: </th>
            <th class="text-right">Subtotal: </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ShoppingCart.ShoppingCartItems)
        {
            <tr>
                <td class="text-center"><a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="RemoveFromShoppingCart" asp-route-productId="@item.Product.ProductId">Decrease Amount</a></td>
                <td class="text-center">@item.Amount</td>
                <td class="text-start">@item.Product.Name</td>
                <td class="text-end">@item.Product.Price.ToString("C")</td>
                <td class="text-end">
                    @((item.Amount * item.Product.Price).ToString("C"))
                </td>
            </tr>
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="4" class="text-end"> Total: </td>
            <td class="text-end"> @Model.ShoppingCartTotal.ToString("C") </td>
        </tr>
    </tfoot>
</table>

<div class="text-center">
    <a class="btn btn-primary" asp-controller="Product" asp-action="List">Continue Shopping</a>
    <a class="btn btn-primary" asp-controller="ShoppingCart" asp-action="ClearShoppingCart">Clear Cart</a>
    <a class="btn btn-primary" asp-controller="Order" asp-action="Checkout">Checkout</a>
</div>